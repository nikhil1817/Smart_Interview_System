import React from 'react';

/**
 * Header Component
 * Top navigation and session info
 */

function Header({ sessionId, timeElapsed, onEndSession }) {
  return (
    <header className="app-header">
      {/* Component content */}
    </header>
  );
}

export default Header;
