/**
 * Utility Functions
 * Helper functions for common operations
 */

export const formatDuration = (seconds) => {
  // Format seconds to HH:MM:SS
};

export const getAgentColor = (agentName) => {
  // Return color based on agent name
};

export const parseEvaluation = (evaluation) => {
  // Parse and format evaluation data
};

export const calculateAverageScore = (scores) => {
  // Calculate average score
};

export default {
  formatDuration,
  getAgentColor,
  parseEvaluation,
  calculateAverageScore,
};
